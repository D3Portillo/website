/**
 * @TODO
 * // * Paragraph Component with sizes
 * // * Layout components
 * // * Image Container bug
 * // * SEO
 * * Mejorar/Pulir las Notas
 * // * /notes page show cropped items
 * // * Last update to footer
 * // * Fix on image focus movement in Homepage
 * // * Add Disqus
 * // * Last Mobile updates
 * // * User can get a Random note read
 * // * Animation/Transition to mobile menu
 * // * Update note cover palette to be brighter
 * // * Add greater z-index to mediumZoom
 * // * services => helpers
 * // * add /hooks
 * // * New mobile navigation
 * // * module to get random module for notes cover
 * // * Reduce note's json payload(image cover removed)
 * // * CV in PDF
 * // * Autogenerates notes cover
 * // * PDF Preview for slides
 * // * Image Optimization
 * // * Mobile version
 * // * Slider bug
 * // * Move shared components to /components
 */
